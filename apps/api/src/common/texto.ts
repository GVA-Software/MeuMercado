// Busca tolerante (acento + abreviação do cupom) — implementação única no domínio,
// compartilhada com o front. Reexportada aqui pra não quebrar os imports da API.
export { semAcento, combinaBusca } from '@meumercado/domain';
